#include <bits/stdc++.h>
using namespace std;
#define int long long
#define endl '\n'
int n, m;
int u, v;
int q;
signed main(){
    ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    cin >> n >> m;
    for(int i = 1; i <= m; i++)
        cin >> u >> v;
    cin >> q;
    while(q--) cout << "NO" << endl;
    return 0;
}